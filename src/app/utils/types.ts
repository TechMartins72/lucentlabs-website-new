import { IconType } from "react-icons";

export type Links = {
  title: string;
  url: string;
};

export type Socials = {
  icon: IconType;
  url: string;
};

export type ModeType = "dark" | "light";
